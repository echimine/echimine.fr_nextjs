// motion-components.ts
'use client'; // si tu es dans un composant client

import { motion } from 'framer-motion';

import { Typographie } from './Typographie';

export const MotionTypographie = motion.create(Typographie);
